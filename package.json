{
  "name": "portfolio",
  "version": "0.1.0",
  "homepage": "https://wantoch.net",
  "scripts": {
    "build": "parcel build src/index.html",
    "watch": "parcel watch src/index.html",
    "dev": "parcel src/index.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "prepush": "npm run deploy"
  },
  "dependencies": {
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "husky": "^1.3.1",
    "parcel-bundler": "^1.11.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
