overrides:
  # Keep Vercel's route compiler on the patched compatible release.
  "@vercel/routing-utils>path-to-regexp": "6.3.0"
onlyBuiltDependencies:
  - esbuild
  - sharp
ignoredBuiltDependencies:
  - "@vercel/speed-insights"
